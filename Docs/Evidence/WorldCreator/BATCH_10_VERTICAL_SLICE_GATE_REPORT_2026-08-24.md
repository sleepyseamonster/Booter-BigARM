# Batch 10 vertical-slice gate report

Date: 2026-08-24

## Outcome

The automated and packaging portion of Batch 10 passes. Final World Creator approval is withheld until the user accepts the production-camera landscape and a Development Player is profiled on the agreed mid-range Windows target.

The production proof remains explicitly non-canon. It does not define the future thematic globe, coordinate bounds, projection, wrapping, notation, named regions, regional map, or lore.

## Production transect proof

`WorldCreatorVerticalSliceProof` executes the production topology-v2 authority across the declared 4,608 m by 4,608 m technical envelope:

- seeds: `24681357`, `3496479`, and `8675309`;
- three transects per seed: horizontal, diagonal/rebase, and offset;
- nine total continuous transects, each 3,072-3,258 m;
- 65 absolute samples per transect;
- nine unique non-identity arrangement signatures and three unique seed source fingerprints;
- height ranges of approximately 93-153 m;
- canyon semantics on every transect;
- Booter and BigARM walkable samples on every transect plus declared reserved-route samples;
- semantic material variation on every transect;
- 79-120 geological formation plans per sampled 576 m owner area, with every reported novelty fingerprint unique inside that area;
- 45 bounded feature requests per central nine-cell proof window, queryable through the production terrain authority;
- one deterministic, seed-varied, explicitly non-canon synthetic causal-history fixture per seed, including site reservation, approach, construction, destruction, burial, and weathering order;
- exact near/mid/far center agreement for absolute position, stable feature identity, source fingerprint, and material semantics;
- successful local-origin rebase without changing canonical surface truth;
- query caches within 96 canyon-plan and 24 terrain-window bounds;
- 27/27 representation builds completed and integrated with no failure or stale rejection;
- zero automated hard violations.

Full numeric receipt: `/tmp/booter-worldcreator-batch10-vertical-slice-report.txt`.

## Version and history authority

- `IWorldHistoryPlanProvider` is the single bounded injection seam through which future site generators can submit causal history before terrain compilation.
- The unbounded production query requests applicable history per terrain window and passes it into `HybridTerrainCompiler`; site content does not edit render meshes or become a second terrain authority.
- The current synthetic fixture is seed-varied and explicitly non-canon.
- Enabling this fixture changes site-domain truth, so the independent production site version advances from `1` to `2`. Existing site-v1 saves fail with a typed manifest mismatch instead of silently moving or reshaping a marked place.
- The production source fingerprint includes material version, site version, and proof-history identity so cached representations cannot survive this change.

## Duplicate-authority audit

- `TopDown3DWorldGenerator` remains a thin compatibility/query adapter and contains no `SampleCore`, fractal-noise, or Perlin macro terrain authority.
- Near terrain, mid/far terrain, semantic material packing, deposited dust, and geological rock realization route through World Creator production services.
- The source validator recursively confirms that no production runtime source consumes the retired `TopDown3DHeightSampler`, escarpment authorities, or `TopDown3DRockFormationPlanner` cell-density authority.
- The old rock planner definition remains because six existing test/tool calls still consume it. Removing it now would destroy compatibility evidence and overlap user-owned test work. It has no production caller and is not a competing runtime authority.
- Existing user-owned worktree deletions of old escarpment/height files were not staged or claimed by this batch.

## Verification receipts

- Batch 10 vertical-slice proof: 1/1 passed (`/tmp/booter-batch10-vertical-slice-sealed.xml`).
- Complete World Creator namespace: 81/81 passed (`/tmp/booter-batch10-worldcreator-sealed.xml`).
- Production topology/world-generator regression: 9/9 passed (`/tmp/booter-batch10-worldgen-sealed.xml`).
- Production path and retired-consumer validator: passed (`/tmp/booter-batch10-validator-sealed.log`).
- Exact Development StandaloneOSX build: succeeded (`/tmp/BooterBigArm-Batch10-Sealed.app`, `/tmp/booter-batch10-development-build-sealed.log`).
- Headless macOS Development Player diagnostic (`/tmp/booter-batch10-development-player-sealed.log`):
  - startup sample: 5.3 FPS while 25 terrain requests were pending;
  - steady samples after terrain integration: 53.8-57.8 FPS;
  - average frame time: 17.29-18.59 ms;
  - p95: 18.58-18.59 ms;
  - p99: 18.79-20.20 ms;
  - CPU sample: 16.67-17.31 ms;
  - managed memory: 15 MiB;
  - 30 loaded chunks, zero pending terrain, 32 renderers, and 30 colliders.

The player diagnostic used the reversible stress profile, null graphics device, 640x480 output, 50% render scale, fast terrain, and no runtime decoration. GPU time, draw calls, triangles, artistic quality, and Windows behavior are not available from this receipt. The already-known missing footstep-particle shader error remains unrelated; Unity analytics endpoints were also unreachable during the diagnostic.

## Acceptance matrix

| Claim | Status | Evidence or remaining gate |
| --- | --- | --- |
| Same world reconstructs | Pass | deterministic plan/query/save tests and unload/rebuild identity |
| Coordinate design remains open | Pass | replaceable interface and non-canon adapter scans |
| Absolute place survives rebasing | Pass | far-address, representation, and saved-place rebase fixtures |
| Canyon systems cross cells | Pass | boundary graph/seam tests and all nine transects |
| Regional influence behavior is deliberate | Automated pass | continuous/hard-boundary fixtures; fixed visual comparison still belongs to visual review |
| Chunks do not author geology | Pass | production ownership validator |
| Saved locations remain real | Pass | schema-v3 round trip, migration/rejection, and atomic mismatch fixtures |
| Near/mid/far show one place | Automated pass | exact data/identity agreement; visible transition review remains visual |
| Traversal remains playable | Partial | graph and agent-affordance proofs pass; hands-on Booter/BigARM traversal remains user-owned |
| Sites can belong to terrain | Pass | live history-provider seam plus bounded synthetic causal fixture |
| No obvious cookie-cutter repetition | Partial | nine arrangement signatures and geological fingerprints pass; perceptual review remains user-owned |
| Views feel composed | Open gate | fixed-camera production-scene review required |
| Visible destinations preserve exploration trust | Open gate | multi-approach production-camera review and walk-through required |
| Density serves spatial purpose | Open gate | hands-on landscape review required |
| Samples scale rules rather than stamps | Automated pass | causal grammar and seed comparison evidence; final visual judgment remains user-owned |
| Streaming work is bounded | Provisional pass | cache/build/integration and macOS diagnostics pass; target-machine profiler capture remains |
| Rendering fits target | Open gate | target Windows hardware, resolution, preset, threshold, GPU/draw/triangle/memory proof required |
| Visual target is achieved | Open gate | user acceptance required |

## Required final review

The exact review setup, seven-view capture matrix, target definition fields, profile sequence, and approval record are in `BATCH_10_VISUAL_AND_TARGET_PROFILE_HANDOFF_2026-08-24.md`.

1. The user opens the production scene and performs the fixed-camera/hands-on landscape review, including canyon reveal/interior, overlook, negative-space basin, geological silhouette, proof-influence transition, and streaming transition.
2. The user defines or supplies the target Windows hardware class and desired resolution/quality/frame-rate threshold; the exact Development build is then profiled there.
3. If both gates pass, Batch 10 and the World Creator foundation can receive final approval. If either fails, the evidence identifies whether the next work belongs to composition, traversal, representation, or performance rather than reopening the architecture blindly.
